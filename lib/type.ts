export type Vendor = {
    id?: string;
    name: string;
    icon?: string;
};

export type HeroItem = {
    id?: string;
    title: string;
    description: string;
    image: string;
    badge?: string;
};
